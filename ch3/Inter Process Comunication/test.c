#include <stdio.h>
#include <unistd.h>

int main()
{
    //printf("dziala main");
    fprintf(stderr, "I will be printed immediately");
while(1)
{
    //printf("petla");
    //sleep(10);
}
    return 0;
}